const header=document.querySelector('header'),menu=document.querySelector('.menu'),nav=document.querySelector('nav'),lang=document.querySelector('#lang');window.addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>24));menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.14});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));let current=localStorage.getItem('siteLanguage')||'en';function apply(l){document.documentElement.lang=l;document.querySelectorAll('[data-en][data-ja]').forEach(e=>e.textContent=e.dataset[l]);lang.textContent=l==='en'?'日本語':'English';localStorage.setItem('siteLanguage',l)}lang.addEventListener('click',()=>{current=current==='en'?'ja':'en';apply(current)});apply(current);document.getElementById('year').textContent=new Date().getFullYear();
